<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.official.noowenz.daggerrxjavamvp">

    <application
        android:name=".utils.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BasicRx.BasicRxJava">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pagination.Pagination" />
        <activity android:name=".registerModule.view.RegisterActivity" />
        <activity android:name=".BasicRx.RxJavaSimpleActivity" />
        <activity android:name=".BasicRx.NamesActivity" />
        <activity android:name=".BasicRx.BooksActivity" />
        <activity android:name=".BasicRx.SchedulerActivity"></activity>
    </application>

</manifest>